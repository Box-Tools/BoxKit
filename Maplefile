# Maplefile for BubbleBox

# Name of the base image
base="ubuntu"

# Name of the local container
container="bubblebox"

# Container backend
backend="docker"

# Default command for execute
execute=["cd cbox/source && make",
         "python3 setup.py develop && python3 setup.py install && ./setup clean"]
